<div class="comment">
  <div>
    <span><%= comment.user.username.capitalize %></span>
    <%= image_tag comment.user.image.url %>
    <span><%= comment.created_at %></span>
  </div>
  <p><%= comment.body %></p>
</div>
