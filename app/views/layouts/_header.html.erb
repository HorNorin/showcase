<nav id="header" class="navbar navbar-inverse navbar-custom">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#menu" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if admin_signed_in? %>
        <%= link_to "Showcase", admin_root_path, class: "navbar-brand" %>
      <% else %>
        <%= link_to "Showcase", root_path, class: "navbar-brand" %>
      <% end %>
    </div>

    <div class="collapse navbar-collapse" id="menu">
      <ul class="nav navbar-nav">
        <li class="dropdown">
          <% if admin_signed_in? %>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dashboard <span class="caret"></span></a>
            <ul class="dropdown-menu dropdown-inverse">
              <li><%= link_to "Show", admin_shows_path %></li>
              <li><%= link_to "User", "#" %></li>
            </ul>
          <% else %>
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Language <span class="caret"></span></a>
            <ul class="dropdown-menu dropdown-inverse">
              <li><a href="#">Action</a></li>
              <li><a href="#">Another action</a></li>
              <li><a href="#">Something else here</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">Separated link</a></li>
              <li role="separator" class="divider"></li>
              <li><a href="#">One more separated link</a></li>
            </ul>
          <% end %>
        </li>
      </ul>
      <%= form_tag shows_path, class: "navbar-form navbar-left", id: "search_form", role: "search", method: :get, "ng-controller" => "ShowsController" do %>
        <div class="form-group left-inner-addon">
          <span class="glyphicon glyphicon-search" aria-hidden="true"></span>
          <%= text_field_tag :title, "", class: "form-control", id: "search", "ng-model" => "show.title", placeholder: "Search" %>
        </div>
      <% end %>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Hi, <%= current_user.username.capitalize %> <span class="caret"></span></a>
            <ul class="dropdown-menu dropdown-inverse">
              <% unless signed_in_with_omniauth? %>
                <li><%= link_to "Setting", edit_user_registration_path %></li>
                <li role="separator" class="divider"></li>
              <% end %>
              <li><%= link_to "Sign Out", sign_out_path %></li>
            </ul>
          </li>
        <% elsif admin_signed_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Hi, <%= current_admin.username.capitalize %> <span class="caret"></span></a>
            <ul class="dropdown-menu dropdown-inverse">
              <li><%= link_to "Sign Out", sign_out_path %></li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to "Sign In", sign_in_path %></li>
          <li><%= link_to "Sign Up", sign_up_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
